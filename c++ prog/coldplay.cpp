#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int m, s, a;
        cin >> m;
        cin >> s;
        a = m / s;
        cout << a << endl;
    }
    return 0;
}